#!/usr/bin/env node

// @flow

const run = require('babel-codemod').default;

run(['--plugin ../src/index.js'].concat(process.argv), process.stdin, process.stdout, process.stderr)
.then(status => {
  process.exit(status);
})
.catch(err => {
  console.error(err.stack);
  process.exit(-1);
});